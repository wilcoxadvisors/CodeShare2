Got it — let’s expand the whitelist so the system will happily accept:


Extension	MIME type(s) we need to allow
.xlsx	application/vnd.openxmlformats-officedocument.spreadsheetml.sheet
.xls	application/vnd.ms-excel
.csv	text/csv (modern browsers)  / application/vnd.ms-excel (older)
.docx	application/vnd.openxmlformats-officedocument.wordprocessingml.document
.doc	application/msword
.eml	message/rfc822 (from the previous request)
1. Backend - add to the allow-list
server/constants/fileTypes.ts

diff
Copy
Edit
 export const ALLOWED_MIME_TYPES = [
   'application/pdf',
   'image/png',
   'image/jpeg',
   'image/gif',
   'message/rfc822', // .eml
+  // Excel / CSV
+  'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet', // .xlsx
+  'application/vnd.ms-excel',            // .xls  (and some csv downloads)
+  'text/csv',                            // .csv
+  // Word
+  'application/vnd.openxmlformats-officedocument.wordprocessingml.document', // .docx
+  'application/msword',                  // .doc
 ];
Nothing else in the server pipeline needs to change—our size limits, virus-scan hook, and S3 storage code all work generically by MIME type.

2. Front-end drop-zone accept map
client/components/AttachmentDropzone.tsx (or wherever the useDropzone lives):

diff
Copy
Edit
-const ACCEPTED = {
-  'application/pdf': ['.pdf'],
-  'image/*': ['.png', '.jpg', '.jpeg', '.gif'],
-  'message/rfc822': ['.eml'],
-};
+const ACCEPTED = {
+  'application/pdf': ['.pdf'],
+  'image/*': ['.png', '.jpg', '.jpeg', '.gif'],
+  'message/rfc822': ['.eml'],
+  // Excel / CSV
+  'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet': ['.xlsx'],
+  'application/vnd.ms-excel': ['.xls', '.csv'],
+  'text/csv': ['.csv'],
+  // Word
+  'application/vnd.openxmlformats-officedocument.wordprocessingml.document': ['.docx'],
+  'application/msword': ['.doc'],
+};
(If your project has a central constant for this, update it there and import it into both front & back ends.)

3. UX tweaks (optional but nice)
Tooltip / helper text – list the new formats so users know what’s allowed

“PDF, Word (.doc/.docx), Excel (.xls/.xlsx), CSV, images, EML”

Unit tests – add one happy-path test that uploads a small .csv or .xlsx fixture and expects 201.

4. Security / size considerations
These are ordinary Office MIME types; your existing ClamAV scan (or whichever antivirus hook you use for PDFs) can scan them untouched. If you don’t scan binaries yet, you might want to add the same hook to keep parity.